From 368037f9a3d950cccae76690c381224018829f68 Mon Sep 17 00:00:00 2001
From: Alistair Leslie-Hughes <leslie_alistair@hotmail.com>
Date: Wed, 22 Jul 2026 09:40:25 +1000
Subject: [PATCH] Updated vkd3d to 3d9c775c58420cf9b235f780c536c43c56cdb590.

---
 libs/vkd3d/include/private/vkd3d_version.h    |   2 +-
 libs/vkd3d/libs/vkd3d-shader/fx.c             |   4 +-
 libs/vkd3d/libs/vkd3d-shader/glsl.c           | 121 ++++++++++++------
 libs/vkd3d/libs/vkd3d-shader/hlsl_codegen.c   | 104 +++++++--------
 libs/vkd3d/libs/vkd3d-shader/ir.c             |  73 +++++------
 libs/vkd3d/libs/vkd3d-shader/tpf.c            |  45 +++----
 .../libs/vkd3d-shader/vkd3d_shader_private.h  |   2 +-
 7 files changed, 180 insertions(+), 171 deletions(-)

diff --git a/libs/vkd3d/include/private/vkd3d_version.h b/libs/vkd3d/include/private/vkd3d_version.h
index 82c26f8e6a3..baef6472ac6 100644
--- a/libs/vkd3d/include/private/vkd3d_version.h
+++ b/libs/vkd3d/include/private/vkd3d_version.h
@@ -1 +1 @@
-#define VKD3D_VCS_ID " (git 1b0924d1)"
+#define VKD3D_VCS_ID " (git 3d9c775c)"
diff --git a/libs/vkd3d/libs/vkd3d-shader/fx.c b/libs/vkd3d/libs/vkd3d-shader/fx.c
index 0d162fb3b39..36a0439f92d 100644
--- a/libs/vkd3d/libs/vkd3d-shader/fx.c
+++ b/libs/vkd3d/libs/vkd3d-shader/fx.c
@@ -3792,7 +3792,7 @@ static void write_fx_4_buffer(struct hlsl_buffer *b, struct fx_write_context *fx
 
     count = 0;
     size = 0;
-    LIST_FOR_EACH_ENTRY(var, &ctx->extern_vars, struct hlsl_ir_var, extern_entry)
+    LIST_FOR_EACH_ENTRY(var, &ctx->globals->vars, struct hlsl_ir_var, scope_entry)
     {
         uint32_t unpacked_size;
 
@@ -3883,7 +3883,7 @@ static void write_objects(struct fx_write_context *fx, bool shared)
     if (shared && !fx->child_effect)
         return;
 
-    LIST_FOR_EACH_ENTRY(var, &ctx->extern_vars, struct hlsl_ir_var, extern_entry)
+    LIST_FOR_EACH_ENTRY(var, &ctx->globals->vars, struct hlsl_ir_var, scope_entry)
     {
         if (!is_supported_object_variable(ctx, var))
             continue;
diff --git a/libs/vkd3d/libs/vkd3d-shader/glsl.c b/libs/vkd3d/libs/vkd3d-shader/glsl.c
index 2792e951c4d..000bccd7a4e 100644
--- a/libs/vkd3d/libs/vkd3d-shader/glsl.c
+++ b/libs/vkd3d/libs/vkd3d-shader/glsl.c
@@ -497,6 +497,62 @@ static void shader_glsl_print_subscript(struct vkd3d_string_buffer *buffer,
     glsl_src_cleanup(&r, &gen->string_buffers);
 }
 
+static void shader_glsl_print_raw_structured_address(struct vkd3d_string_buffer *buffer,
+        struct vkd3d_glsl_generator *gen, size_t stride, const struct vsir_src_operand *index,
+        uint32_t index_mask, const struct vsir_src_operand *offset, uint32_t offset_mask)
+{
+    bool const_index = false, const_offset = false;
+    int v_index, v_offset;
+    unsigned int swizzle;
+
+    if (!stride)
+    {
+        const_index = true;
+        v_index = 0;
+    }
+    else if (index->reg.type == VSIR_REGISTER_IMMCONST)
+    {
+        const_index = true;
+        swizzle = vsir_swizzle_get_component(index->swizzle, vkd3d_ctz(index_mask));
+        v_index = index->reg.u.immconst_u32[swizzle] * stride;
+    }
+
+    if (offset->reg.type == VSIR_REGISTER_IMMCONST)
+    {
+        const_offset = true;
+        swizzle = vsir_swizzle_get_component(offset->swizzle, vkd3d_ctz(offset_mask));
+        v_offset = offset->reg.u.immconst_u32[swizzle] / 4;
+    }
+
+    if (const_index && const_offset)
+    {
+        vkd3d_string_buffer_printf(buffer, "%d", v_index + v_offset);
+        return;
+    }
+
+    if (!const_index)
+    {
+        shader_glsl_print_src(buffer, gen, index, index_mask, VSIR_DATA_I32);
+        vkd3d_string_buffer_printf(buffer, " * %zu", stride);
+        if (!const_offset || v_offset)
+            vkd3d_string_buffer_printf(buffer, " + ");
+    }
+    else if (v_index)
+    {
+        vkd3d_string_buffer_printf(buffer, "%d + ", v_index);
+    }
+
+    if (!const_offset)
+    {
+        shader_glsl_print_src(buffer, gen, offset, offset_mask, VSIR_DATA_I32);
+        vkd3d_string_buffer_printf(buffer, " / 4");
+    }
+    else if (v_offset)
+    {
+        vkd3d_string_buffer_printf(buffer, "%d", v_offset);
+    }
+}
+
 static void VKD3D_PRINTF_FUNC(4, 0) shader_glsl_vprint_assignment(struct vkd3d_glsl_generator *gen,
         struct glsl_dst *dst, enum vsir_data_type data_type, const char *format, va_list args)
 {
@@ -649,13 +705,9 @@ static void shader_glsl_atomic(struct vkd3d_glsl_generator *gen,
         shader_glsl_print_image_name(buffer, gen, resource_idx, resource_space);
         vkd3d_string_buffer_printf(buffer, ", ");
     }
-    if (stride)
-    {
-        shader_glsl_print_src(buffer, gen, &ins->src[0], VKD3DSP_WRITEMASK_0, VSIR_DATA_I32);
-        vkd3d_string_buffer_printf(buffer, " * %u + ", stride);
-    }
-    shader_glsl_print_src(buffer, gen, &ins->src[0], VKD3DSP_WRITEMASK_1, VSIR_DATA_I32);
-    vkd3d_string_buffer_printf(buffer, " / 4%s, ", is_tgsm ? "]" : "");
+    shader_glsl_print_raw_structured_address(buffer, gen, stride, &ins->src[0],
+            VKD3DSP_WRITEMASK_0, &ins->src[0], VKD3DSP_WRITEMASK_1);
+    vkd3d_string_buffer_printf(buffer, "%s, ", is_tgsm ? "]" : "");
     shader_glsl_print_src(buffer, gen, &ins->src[1], VKD3DSP_WRITEMASK_0, data_type);
     if (ins->src_count >= 3)
     {
@@ -1190,8 +1242,8 @@ static void shader_glsl_sample(struct vkd3d_glsl_generator *gen, const struct vk
 static void shader_glsl_load_raw_structured(struct vkd3d_glsl_generator *gen,
         const struct vkd3d_shader_instruction *ins)
 {
-    unsigned int count, descriptor_id, resource_idx, resource_space, i, stride, src_idx = 0;
     const struct vsir_src_operand *resource_src = &ins->src[ins->src_count - 1];
+    unsigned int count, descriptor_id, resource_idx, resource_space, i, stride;
     struct vkd3d_string_buffer *coord, *load;
     uint32_t dst_mask, mask, swizzle;
     const struct vsir_descriptor *d;
@@ -1250,12 +1302,10 @@ static void shader_glsl_load_raw_structured(struct vkd3d_glsl_generator *gen,
     load = vkd3d_string_buffer_get(&gen->string_buffers);
 
     if (ins->opcode == VSIR_OP_LD_STRUCTURED)
-    {
-        shader_glsl_print_src(coord, gen, &ins->src[src_idx++], VKD3DSP_WRITEMASK_0, VSIR_DATA_I32);
-        vkd3d_string_buffer_printf(coord, " * %u + ", stride);
-    }
-    shader_glsl_print_src(coord, gen, &ins->src[src_idx++], VKD3DSP_WRITEMASK_0, VSIR_DATA_I32);
-    vkd3d_string_buffer_printf(coord, " / 4");
+        shader_glsl_print_raw_structured_address(coord, gen, stride, &ins->src[0],
+                VKD3DSP_WRITEMASK_0, &ins->src[1], VKD3DSP_WRITEMASK_0);
+    else
+        shader_glsl_print_raw_structured_address(coord, gen, 0, NULL, 0, &ins->src[0], VKD3DSP_WRITEMASK_0);
 
     dst_mask = ins->dst[0].write_mask;
     count = vsir_write_mask_component_count(dst_mask);
@@ -1357,7 +1407,7 @@ static void shader_glsl_load_uav_typed(struct vkd3d_glsl_generator *gen, const s
 static void shader_glsl_store_raw_structured(struct vkd3d_glsl_generator *gen,
         const struct vkd3d_shader_instruction *ins)
 {
-    unsigned int descriptor_id, resource_idx, resource_space, i, stride, src_idx = 0;
+    unsigned int descriptor_id, resource_idx, resource_space, i, stride, src_idx;
     struct vkd3d_string_buffer *buffer = gen->buffer;
     struct vkd3d_string_buffer *coord;
     const struct vsir_descriptor *d;
@@ -1415,11 +1465,15 @@ static void shader_glsl_store_raw_structured(struct vkd3d_glsl_generator *gen,
 
     if (ins->opcode == VSIR_OP_STORE_STRUCTURED)
     {
-        shader_glsl_print_src(coord, gen, &ins->src[src_idx++], VKD3DSP_WRITEMASK_0, VSIR_DATA_I32);
-        vkd3d_string_buffer_printf(coord, " * %u + ", stride);
+        shader_glsl_print_raw_structured_address(coord, gen, stride, &ins->src[0],
+                VKD3DSP_WRITEMASK_0, &ins->src[1], VKD3DSP_WRITEMASK_0);
+        src_idx = 2;
+    }
+    else
+    {
+        shader_glsl_print_raw_structured_address(coord, gen, 0, NULL, 0, &ins->src[0], VKD3DSP_WRITEMASK_0);
+        src_idx = 1;
     }
-    shader_glsl_print_src(coord, gen, &ins->src[src_idx++], VKD3DSP_WRITEMASK_0, VSIR_DATA_I32);
-    vkd3d_string_buffer_printf(coord, " / 4");
 
     for (i = 0; i < VKD3D_VEC4_SIZE; ++i)
     {
@@ -1677,9 +1731,6 @@ static void shader_glsl_shader_prologue(struct vkd3d_glsl_generator *gen)
     {
         e = &signature->elements[i];
 
-        if (e->target_location == SIGNATURE_TARGET_LOCATION_UNUSED)
-            continue;
-
         for (unsigned int j = 0; j < e->register_count; ++j)
         {
             shader_glsl_print_indent(buffer, gen->indent);
@@ -1687,6 +1738,9 @@ static void shader_glsl_shader_prologue(struct vkd3d_glsl_generator *gen)
             shader_glsl_print_write_mask(buffer, e->mask);
             if (e->sysval_semantic == VKD3D_SHADER_SV_NONE)
             {
+                if (e->target_location == SIGNATURE_TARGET_LOCATION_UNUSED)
+                    break;
+
                 if (gen->interstage_input)
                 {
                     vkd3d_string_buffer_printf(buffer, " = shader_in.reg_%u", e->target_location + j);
@@ -1739,13 +1793,16 @@ static void shader_glsl_shader_epilogue(struct vkd3d_glsl_generator *gen)
     {
         e = &signature->elements[i];
 
-        if (e->target_location == SIGNATURE_TARGET_LOCATION_UNUSED)
+        if (!gen->interstage_output && e->sysval_semantic != VKD3D_SHADER_SV_TARGET)
             continue;
 
         type = e->component_type;
         shader_glsl_print_indent(buffer, gen->indent);
         if (e->sysval_semantic == VKD3D_SHADER_SV_NONE)
         {
+            if (e->target_location == SIGNATURE_TARGET_LOCATION_UNUSED)
+                continue;
+
             if (gen->interstage_output)
             {
                 type = VKD3D_SHADER_COMPONENT_FLOAT;
@@ -1797,14 +1854,9 @@ static void shader_glsl_shader_epilogue(struct vkd3d_glsl_generator *gen)
 
 static void shader_glsl_ret(struct vkd3d_glsl_generator *gen, const struct vkd3d_shader_instruction *ins)
 {
-    const struct vkd3d_shader_version *version = &gen->program->shader_version;
-
-    if (version->major >= 4)
-    {
-        shader_glsl_shader_epilogue(gen);
-        shader_glsl_print_indent(gen->buffer, gen->indent);
-        vkd3d_string_buffer_printf(gen->buffer, "return;\n");
-    }
+    shader_glsl_shader_epilogue(gen);
+    shader_glsl_print_indent(gen->buffer, gen->indent);
+    vkd3d_string_buffer_printf(gen->buffer, "return;\n");
 }
 
 static void shader_glsl_dcl_indexable_temp(struct vkd3d_glsl_generator *gen,
@@ -2684,15 +2736,8 @@ static void shader_glsl_generate_output_declarations(struct vkd3d_glsl_generator
         {
             e = &signature->elements[i];
 
-            if (e->target_location == SIGNATURE_TARGET_LOCATION_UNUSED)
-                continue;
-
             if (e->sysval_semantic != VKD3D_SHADER_SV_TARGET)
-            {
-                vkd3d_glsl_compiler_error(gen, VKD3D_SHADER_ERROR_GLSL_INTERNAL,
-                        "Internal compiler error: Unhandled system value %#x.", e->sysval_semantic);
                 continue;
-            }
 
             if (e->min_precision != VKD3D_SHADER_MINIMUM_PRECISION_NONE)
             {
diff --git a/libs/vkd3d/libs/vkd3d-shader/hlsl_codegen.c b/libs/vkd3d/libs/vkd3d-shader/hlsl_codegen.c
index f1e28283a23..4a4be014c50 100644
--- a/libs/vkd3d/libs/vkd3d-shader/hlsl_codegen.c
+++ b/libs/vkd3d/libs/vkd3d-shader/hlsl_codegen.c
@@ -7778,8 +7778,6 @@ static struct vsir_signature_element *generate_signature_entry(struct hlsl_ctx *
     }
     else
     {
-        bool has_idx;
-
         if (!sm4_sysval_semantic_from_semantic_name(&sysval, &version,
                 ctx->compile_info.map_semantic_names, ctx->domain, var->semantic.name,
                 var->semantic.index, output, ctx->is_patch_constant_func, is_primitive))
@@ -7789,8 +7787,8 @@ static struct vsir_signature_element *generate_signature_entry(struct hlsl_ctx *
             return NULL;
         }
 
-        if ((builtin = sm4_register_from_semantic_name(&version, var->semantic.name, output, &type, &has_idx)))
-            reg = has_idx ? var->semantic.index : ~0u;
+        if ((builtin = sm4_register_from_semantic_name(&version, var->semantic.name, output, &type)))
+            reg = ~0u;
 
         if (sysval == VKD3D_SHADER_SV_STENCIL_REF)
         {
@@ -7871,8 +7869,20 @@ static struct vsir_signature_element *generate_signature_entry(struct hlsl_ctx *
 
     if (builtin)
     {
-        TRACE("%s %s semantic %s[%u] matches predefined register %#x[%u].\n", shader_names[version.type],
-                output ? "output" : "input", var->semantic.name, var->semantic.index, type, reg);
+        TRACE("%s %s semantic %s matches predefined register %#x.\n", shader_names[version.type],
+                output ? "output" : "input", var->semantic.name, type);
+    }
+    else if (sysval == VKD3D_SHADER_SV_TARGET)
+    {
+        /* TARGET uniquely gets allocated to the register corresponding to the semantic index. */
+
+        var->regs[HLSL_REGSET_NUMERIC].type = VSIR_REGISTER_OUTPUT;
+        var->regs[HLSL_REGSET_NUMERIC].allocated = true;
+        var->regs[HLSL_REGSET_NUMERIC].allocation_size = 1;
+        var->regs[HLSL_REGSET_NUMERIC].id = var->semantic.index;
+        var->regs[HLSL_REGSET_NUMERIC].writemask = (1u << var->data_type->e.numeric.dimx) - 1;
+
+        TRACE("Pixel output semantic %s[%u] is colour output.\n", var->semantic.name, var->semantic.index);
     }
     else
     {
@@ -7965,6 +7975,8 @@ static struct vsir_signature_element *generate_signature_entry(struct hlsl_ctx *
             element->min_precision = VKD3D_SHADER_MINIMUM_PRECISION_UINT_16;
             break;
     }
+    if (element->min_precision != VKD3D_SHADER_MINIMUM_PRECISION_NONE || base_type->is_minimum_precision)
+        program->global_flags |= VKD3DSGF_ENABLE_MINIMUM_PRECISION;
 
     return element;
 }
@@ -10040,20 +10052,13 @@ static bool sm4_generate_vsir_reg_from_deref(struct hlsl_ctx *ctx, struct vsir_p
     else if (var->is_input_semantic)
     {
         bool is_primitive = hlsl_type_is_primitive_array(var->data_type);
-        bool has_idx;
 
-        if (sm4_register_from_semantic_name(version, var->semantic.name, false, &reg->type, &has_idx))
+        if (sm4_register_from_semantic_name(version, var->semantic.name, false, &reg->type))
         {
             unsigned int offset = hlsl_offset_from_deref_safe(ctx, deref);
 
             VKD3D_ASSERT(!is_primitive);
 
-            if (has_idx)
-            {
-                reg->idx[0].offset = var->semantic.index + offset / 4;
-                reg->idx_count = 1;
-            }
-
             if (shader_sm4_is_scalar_register(reg))
                 reg->dimension = VSIR_DIMENSION_SCALAR;
             else
@@ -10085,18 +10090,10 @@ static bool sm4_generate_vsir_reg_from_deref(struct hlsl_ctx *ctx, struct vsir_p
     }
     else if (var->is_output_semantic)
     {
-        bool has_idx;
-
-        if (sm4_register_from_semantic_name(version, var->semantic.name, true, &reg->type, &has_idx))
+        if (sm4_register_from_semantic_name(version, var->semantic.name, true, &reg->type))
         {
             unsigned int offset = hlsl_offset_from_deref_safe(ctx, deref);
 
-            if (has_idx)
-            {
-                reg->idx[0].offset = var->semantic.index + offset / 4;
-                reg->idx_count = 1;
-            }
-
             if (shader_sm4_is_scalar_register(reg))
                 reg->dimension = VSIR_DIMENSION_SCALAR;
             else
@@ -10702,14 +10699,9 @@ err:
 static void sm1_generate_vsir_init_dst_operand_from_deref(struct hlsl_ctx *ctx, struct vsir_dst_operand *dst,
         struct hlsl_deref *deref, const struct vkd3d_shader_location *loc, unsigned int writemask)
 {
-    enum vsir_register_type type = VSIR_REGISTER_TEMP;
     struct vkd3d_shader_version version;
-    uint32_t register_index;
-    struct hlsl_reg reg;
-
-    reg = hlsl_reg_from_deref(ctx, deref);
-    register_index = reg.id;
-    writemask = hlsl_combine_writemasks(reg.writemask, writemask);
+    uint32_t register_index, alloc_mask;
+    enum vsir_register_type type;
 
     if (deref->var->is_output_semantic)
     {
@@ -10722,23 +10714,33 @@ static void sm1_generate_vsir_init_dst_operand_from_deref(struct hlsl_ctx *ctx,
         if (!sm1_register_from_semantic_name(&version, semantic_name,
                 deref->var->semantic.index, true, NULL, &type, &register_index))
         {
-            VKD3D_ASSERT(reg.allocated);
+            struct hlsl_reg reg = hlsl_reg_from_deref(ctx, deref);
+
             type = VSIR_REGISTER_OUTPUT;
             register_index = reg.id;
+            alloc_mask = reg.writemask;
         }
         else
-            writemask = (1u << deref->var->data_type->e.numeric.dimx) - 1;
+        {
+            alloc_mask = (1u << deref->var->data_type->e.numeric.dimx) - 1;
+        }
 
         if (version.type == VKD3D_SHADER_TYPE_VERTEX && (!ascii_strcasecmp(semantic_name, "PSIZE")
                 || (!ascii_strcasecmp(semantic_name, "FOG") && version.major < 3)))
         {
             /* These are always 1-component, but for some reason are written
              * with a writemask containing all components. */
-            writemask = VKD3DSP_WRITEMASK_ALL;
+            alloc_mask = VKD3DSP_WRITEMASK_ALL;
         }
     }
     else
-        VKD3D_ASSERT(reg.allocated);
+    {
+        struct hlsl_reg reg = hlsl_reg_from_deref(ctx, deref);
+
+        type = VSIR_REGISTER_TEMP;
+        register_index = reg.id;
+        alloc_mask = reg.writemask;
+    }
 
     if (type == VSIR_REGISTER_DEPTHOUT)
     {
@@ -10751,7 +10753,7 @@ static void sm1_generate_vsir_init_dst_operand_from_deref(struct hlsl_ctx *ctx,
         dst->reg.idx[0].offset = register_index;
         dst->reg.dimension = VSIR_DIMENSION_VEC4;
     }
-    dst->write_mask = writemask;
+    dst->write_mask = hlsl_combine_writemasks(alloc_mask, writemask);
 
     if (deref->rel_offset.node)
         hlsl_fixme(ctx, loc, "Translate relative addressing on dst register for vsir.");
@@ -11775,10 +11777,9 @@ static void sm4_generate_vsir_instr_dcl_semantic(struct hlsl_ctx *ctx, struct vs
             opcode = VSIR_OP_DCL_OUTPUT_SIV;
     }
 
-    if (sm4_register_from_semantic_name(version, var->semantic.name, output, &type, &has_idx))
+    if (sm4_register_from_semantic_name(version, var->semantic.name, output, &type))
     {
-        if (has_idx)
-            idx = var->semantic.index;
+        has_idx = false;
         write_mask = (1u << var->data_type->e.numeric.dimx) - 1;
     }
     else
@@ -13725,13 +13726,12 @@ static void generate_vsir_scan_required_features(struct hlsl_ctx *ctx, struct vs
      * STENCIL_REF, and TYPED_UAV_LOAD_ADDITIONAL_FORMATS. */
 }
 
-static void generate_vsir_scan_global_flags(struct hlsl_ctx *ctx, struct vsir_program *program,
-        const struct list *semantic_vars, const struct hlsl_ir_function_decl *entry_func)
+static void generate_vsir_scan_global_flags(struct hlsl_ctx *ctx,
+        struct vsir_program *program, const struct hlsl_ir_function_decl *entry_func)
 {
     const struct vkd3d_shader_version *version = &program->shader_version;
     struct extern_resource *extern_resources;
     unsigned int extern_resources_count, i;
-    struct hlsl_ir_var *var;
 
     extern_resources = sm4_get_extern_resources(ctx, &extern_resources_count);
 
@@ -13752,24 +13752,8 @@ static void generate_vsir_scan_global_flags(struct hlsl_ctx *ctx, struct vsir_pr
 
     sm4_free_extern_resources(extern_resources, extern_resources_count);
 
-    LIST_FOR_EACH_ENTRY(var, semantic_vars, struct hlsl_ir_var, extern_entry)
-    {
-        const struct hlsl_type *type = var->data_type;
-
-        if (hlsl_type_is_primitive_array(type))
-            type = var->data_type->e.array.type;
-
-        /* Note that it doesn't matter if the semantic is unused or doesn't
-         * generate a signature element (e.g. SV_DispatchThreadID). */
-        if ((var->is_input_semantic || var->is_output_semantic)
-                && (type->is_minimum_precision || hlsl_type_is_minimum_precision(type)))
-        {
-            program->global_flags |= VKD3DSGF_ENABLE_MINIMUM_PRECISION;
-            break;
-        }
-    }
-    /* FIXME: We also need to check for minimum-precision uniforms and local
-     * variable arithmetic. */
+    /* FIXME: Check for minimum-precision uniforms and local variable
+     * arithmetic, and enable VKD3DSGF_ENABLE_MINIMUM_PRECISION if so. */
 
     if (entry_func->early_depth_test && vkd3d_shader_ver_ge(version, 5, 0))
         program->global_flags |= VKD3DSGF_FORCE_EARLY_DEPTH_STENCIL;
@@ -14174,7 +14158,7 @@ static void sm4_generate_vsir(struct hlsl_ctx *ctx,
     }
 
     generate_vsir_scan_required_features(ctx, program);
-    generate_vsir_scan_global_flags(ctx, program, semantic_vars, func);
+    generate_vsir_scan_global_flags(ctx, program, func);
 
     program->ssa_count = ctx->ssa_count;
     program->temp_count = ctx->temp_count;
diff --git a/libs/vkd3d/libs/vkd3d-shader/ir.c b/libs/vkd3d/libs/vkd3d-shader/ir.c
index 89981b564e6..3f6eaf123b1 100644
--- a/libs/vkd3d/libs/vkd3d-shader/ir.c
+++ b/libs/vkd3d/libs/vkd3d-shader/ir.c
@@ -3683,28 +3683,26 @@ static struct vkd3d_shader_instruction *vsir_program_iterator_dcl_indexable_temp
 static enum vkd3d_result vsir_program_copy_d3dbc_input_to_indexable_temp(struct vsir_program *program,
         struct vsir_transformation_context *ctx, unsigned int *idxtemp_idx)
 {
-    struct vsir_program_iterator dcl_it = vsir_program_iterator(&program->instructions);
     struct vsir_program_iterator mov_it = vsir_program_iterator(&program->instructions);
     struct vsir_program_iterator it = vsir_program_iterator(&program->instructions);
     struct vkd3d_shader_location loc = ctx->null_location;
-    struct vkd3d_shader_instruction *ins, *dcl_ins;
-    struct vkd3d_shader_semantic *semantic;
+    struct vkd3d_shader_instruction *ins;
     size_t register_size = 0;
-    size_t dcl_count = 0;
 
     *idxtemp_idx = vsir_program_get_idxtemp_count(program);
 
-    for (ins = vsir_program_iterator_head(&it); ins; ins = vsir_program_iterator_next(&it))
+    for (unsigned int i = 0; i < program->input_signature.element_count; ++i)
     {
-        if (ins->opcode == VSIR_OP_DCL)
-        {
-            semantic = &ins->declaration.semantic;
-            if (semantic->resource.reg.reg.type != VSIR_REGISTER_INPUT)
-                continue;
-            register_size = max(register_size, semantic->resource.reg.reg.idx[0].offset + 1);
-            ++dcl_count;
-        }
+        const struct vsir_signature_element *e = &program->input_signature.elements[i];
+
+        register_size = max(register_size, e->register_index + 1);
+    }
 
+    if (!register_size)
+        return VKD3D_OK;
+
+    for (ins = vsir_program_iterator_head(&it); ins; ins = vsir_program_iterator_next(&it))
+    {
         if (!vsir_instruction_is_dcl(ins) && ins->opcode != VSIR_OP_NOP)
         {
             loc = ins->location;
@@ -3712,48 +3710,37 @@ static enum vkd3d_result vsir_program_copy_d3dbc_input_to_indexable_temp(struct
         }
     }
 
-    if (!register_size)
-        return VKD3D_OK;
     if (!vsir_program_iterator_dcl_indexable_temp_before(ctx, &it, *idxtemp_idx, register_size))
         return VKD3D_ERROR_OUT_OF_MEMORY;
-    if (!(ins = vsir_program_iterator_insert_before(&it, &mov_it, dcl_count)))
+    if (!(ins = vsir_program_iterator_insert_before(&it, &mov_it, program->input_signature.element_count)))
         return VKD3D_ERROR_OUT_OF_MEMORY;
 
-    for (dcl_ins = vsir_program_iterator_head(&dcl_it); dcl_ins; dcl_ins = vsir_program_iterator_next(&dcl_it))
+    for (unsigned int i = 0; i < program->input_signature.element_count; ++i)
     {
+        const struct vsir_signature_element *e = &program->input_signature.elements[i];
         struct vsir_dst_operand *dst;
         struct vsir_src_operand *src;
 
-        if (dcl_ins->opcode == VSIR_OP_DCL)
-        {
-            semantic = &dcl_ins->declaration.semantic;
-            if (semantic->resource.reg.reg.type != VSIR_REGISTER_INPUT)
-                continue;
-
-            if (!vsir_instruction_init_with_params(program, ins, &dcl_ins->location, VSIR_OP_MOV, 1, 1))
-                goto fail;
-
-            dst = &ins->dst[0];
-            vsir_dst_operand_init(dst, VSIR_REGISTER_IDXTEMP, VSIR_DATA_F32, 2);
-            dst->reg.idx[0].offset = *idxtemp_idx;
-            dst->reg.idx[1].offset = semantic->resource.reg.reg.idx[0].offset;
-            dst->reg.dimension = VSIR_DIMENSION_VEC4;
-            dst->write_mask = semantic->resource.reg.write_mask;
+        if (!vsir_instruction_init_with_params(program, ins, &loc, VSIR_OP_MOV, 1, 1))
+            goto fail;
 
-            src = &ins->src[0];
-            vsir_src_operand_init(src, VSIR_REGISTER_INPUT, VSIR_DATA_F32, 1);
-            src->reg.idx[0].offset = semantic->resource.reg.reg.idx[0].offset;
-            src->reg.dimension = VSIR_DIMENSION_VEC4;
-            src->modifiers = 0;
-            src->swizzle = vsir_swizzle_from_writemask(semantic->resource.reg.write_mask);
+        dst = &ins->dst[0];
+        vsir_dst_operand_init(dst, VSIR_REGISTER_IDXTEMP, VSIR_DATA_F32, 2);
+        dst->reg.idx[0].offset = *idxtemp_idx;
+        dst->reg.idx[1].offset = e->register_index;
+        dst->reg.dimension = VSIR_DIMENSION_VEC4;
+        dst->write_mask = e->mask;
 
-            ins = vsir_program_iterator_next(&mov_it);
+        src = &ins->src[0];
+        vsir_src_operand_init(src, VSIR_REGISTER_INPUT, VSIR_DATA_F32, 1);
+        src->reg.idx[0].offset = e->register_index;
+        src->reg.dimension = VSIR_DIMENSION_VEC4;
+        src->modifiers = 0;
+        src->swizzle = vsir_swizzle_from_writemask(e->mask);
 
-            if (--dcl_count == 0)
-                return VKD3D_OK;
-        }
+        ins = vsir_program_iterator_next(&mov_it);
     }
-    vkd3d_unreachable();
+    return VKD3D_OK;
 
 fail:
     vsir_program_iterator_prev(&it);
diff --git a/libs/vkd3d/libs/vkd3d-shader/tpf.c b/libs/vkd3d/libs/vkd3d-shader/tpf.c
index d79cdc119c2..eddf4ffb06c 100644
--- a/libs/vkd3d/libs/vkd3d-shader/tpf.c
+++ b/libs/vkd3d/libs/vkd3d-shader/tpf.c
@@ -2970,7 +2970,7 @@ int tpf_parse(const struct vkd3d_shader_compile_info *compile_info, uint64_t con
 }
 
 bool sm4_register_from_semantic_name(const struct vkd3d_shader_version *version,
-        const char *semantic_name, bool output, enum vsir_register_type *type, bool *has_idx)
+        const char *semantic_name, bool output, enum vsir_register_type *type)
 {
     unsigned int i;
 
@@ -2980,33 +2980,27 @@ bool sm4_register_from_semantic_name(const struct vkd3d_shader_version *version,
         bool output;
         enum vkd3d_shader_type shader_type;
         enum vsir_register_type type;
-        bool has_idx;
     }
     register_table[] =
     {
-        {"sv_dispatchthreadid",     false, VKD3D_SHADER_TYPE_COMPUTE,  VSIR_REGISTER_THREADID,         false},
-        {"sv_groupid",              false, VKD3D_SHADER_TYPE_COMPUTE,  VSIR_REGISTER_THREADGROUPID,    false},
-        {"sv_groupindex",           false, VKD3D_SHADER_TYPE_COMPUTE,  VSIR_REGISTER_LOCALTHREADINDEX, false},
-        {"sv_groupthreadid",        false, VKD3D_SHADER_TYPE_COMPUTE,  VSIR_REGISTER_LOCALTHREADID,    false},
-
-        {"sv_domainlocation",       false, VKD3D_SHADER_TYPE_DOMAIN,   VSIR_REGISTER_TESSCOORD,        false},
-        {"sv_primitiveid",          false, VKD3D_SHADER_TYPE_DOMAIN,   VSIR_REGISTER_PRIMID,           false},
-
-        {"sv_primitiveid",          false, VKD3D_SHADER_TYPE_GEOMETRY, VSIR_REGISTER_PRIMID,           false},
-        {"sv_gsinstanceid",         false, VKD3D_SHADER_TYPE_GEOMETRY, VSIR_REGISTER_GSINSTID,         false},
-
-        {"sv_outputcontrolpointid", false, VKD3D_SHADER_TYPE_HULL,     VSIR_REGISTER_OUTPOINTID,       false},
-        {"sv_primitiveid",          false, VKD3D_SHADER_TYPE_HULL,     VSIR_REGISTER_PRIMID,           false},
-
-        /* Put sv_target in this table, instead of letting it fall through to
-         * default varying allocation, so that the register index matches the
-         * usage index. */
-        {"color",                   true,  VKD3D_SHADER_TYPE_PIXEL,    VSIR_REGISTER_OUTPUT,           true},
-        {"depth",                   true,  VKD3D_SHADER_TYPE_PIXEL,    VSIR_REGISTER_DEPTHOUT,         false},
-        {"sv_depth",                true,  VKD3D_SHADER_TYPE_PIXEL,    VSIR_REGISTER_DEPTHOUT,         false},
-        {"sv_target",               true,  VKD3D_SHADER_TYPE_PIXEL,    VSIR_REGISTER_OUTPUT,           true},
-        {"sv_coverage",             true,  VKD3D_SHADER_TYPE_PIXEL,    VSIR_REGISTER_SAMPLEMASK,       false},
-        {"sv_stencilref",           true,  VKD3D_SHADER_TYPE_PIXEL,    VSIR_REGISTER_OUTSTENCILREF,    false},
+        {"sv_dispatchthreadid",     false, VKD3D_SHADER_TYPE_COMPUTE,  VSIR_REGISTER_THREADID},
+        {"sv_groupid",              false, VKD3D_SHADER_TYPE_COMPUTE,  VSIR_REGISTER_THREADGROUPID},
+        {"sv_groupindex",           false, VKD3D_SHADER_TYPE_COMPUTE,  VSIR_REGISTER_LOCALTHREADINDEX},
+        {"sv_groupthreadid",        false, VKD3D_SHADER_TYPE_COMPUTE,  VSIR_REGISTER_LOCALTHREADID},
+
+        {"sv_domainlocation",       false, VKD3D_SHADER_TYPE_DOMAIN,   VSIR_REGISTER_TESSCOORD},
+        {"sv_primitiveid",          false, VKD3D_SHADER_TYPE_DOMAIN,   VSIR_REGISTER_PRIMID},
+
+        {"sv_primitiveid",          false, VKD3D_SHADER_TYPE_GEOMETRY, VSIR_REGISTER_PRIMID},
+        {"sv_gsinstanceid",         false, VKD3D_SHADER_TYPE_GEOMETRY, VSIR_REGISTER_GSINSTID},
+
+        {"sv_outputcontrolpointid", false, VKD3D_SHADER_TYPE_HULL,     VSIR_REGISTER_OUTPOINTID},
+        {"sv_primitiveid",          false, VKD3D_SHADER_TYPE_HULL,     VSIR_REGISTER_PRIMID},
+
+        {"depth",                   true,  VKD3D_SHADER_TYPE_PIXEL,    VSIR_REGISTER_DEPTHOUT},
+        {"sv_depth",                true,  VKD3D_SHADER_TYPE_PIXEL,    VSIR_REGISTER_DEPTHOUT},
+        {"sv_coverage",             true,  VKD3D_SHADER_TYPE_PIXEL,    VSIR_REGISTER_SAMPLEMASK},
+        {"sv_stencilref",           true,  VKD3D_SHADER_TYPE_PIXEL,    VSIR_REGISTER_OUTSTENCILREF},
     };
 
     for (i = 0; i < ARRAY_SIZE(register_table); ++i)
@@ -3017,7 +3011,6 @@ bool sm4_register_from_semantic_name(const struct vkd3d_shader_version *version,
         {
             if (type)
                 *type = register_table[i].type;
-            *has_idx = register_table[i].has_idx;
             return true;
         }
     }
diff --git a/libs/vkd3d/libs/vkd3d-shader/vkd3d_shader_private.h b/libs/vkd3d/libs/vkd3d-shader/vkd3d_shader_private.h
index 6fb089b7d74..65b499879e5 100644
--- a/libs/vkd3d/libs/vkd3d-shader/vkd3d_shader_private.h
+++ b/libs/vkd3d/libs/vkd3d-shader/vkd3d_shader_private.h
@@ -1994,7 +1994,7 @@ bool sm1_register_from_semantic_name(const struct vkd3d_shader_version *version,
 bool sm1_usage_from_semantic_name(const struct vkd3d_shader_version *version, const char *semantic_name,
         uint32_t semantic_index, bool output, enum vkd3d_decl_usage *usage, uint32_t *usage_idx);
 bool sm4_register_from_semantic_name(const struct vkd3d_shader_version *version,
-        const char *semantic_name, bool output, enum vsir_register_type *type, bool *has_idx);
+        const char *semantic_name, bool output, enum vsir_register_type *type);
 bool shader_sm4_is_scalar_register(const struct vsir_operand *reg);
 bool sm4_sysval_semantic_from_semantic_name(enum vkd3d_shader_sysval_semantic *sysval_semantic,
         const struct vkd3d_shader_version *version, bool semantic_compat_mapping,
-- 
2.53.0

